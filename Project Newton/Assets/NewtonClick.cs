using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewtonClick : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*/
		int clicks=Input.touchCount;
		Vector3 clickPos;
		if(clicks>0){
			clickPos=Input.GetTouch(0).position;
		}
		RaycastHit hit;
		Ray ray=Camera.main.ScreenPointToRay(clickPos);
		if(Physics.Raycast(ray, out hit) && (clicks>0)){
			Debug.Log("Radi");
		}
		*/
	}
}
